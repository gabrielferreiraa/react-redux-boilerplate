'use strict'

const reducer = 'sidebar'

export const FETCH = `${reducer}/FETCH`
export const SEARCH = `${reducer}/SEARCH`
export const SELECT = `${reducer}/SELECT`
export const TOGGLE = `${reducer}/TOGGLE`
